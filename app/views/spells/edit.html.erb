<p id="notice"><%= flash[:notice] %></p>

<h1>
  <strong>View</strong>
  <%= @spell.name %>
</h1>

<p>
<%= link_to 'All Spells', spells_path %> |
<%= link_to 'Home', root_path %> 
</p>

<%= render 'form', spell: @spell %>


<%= form_with model: @spell, method: :delete, local: true do |del| %><td>
    <%= del.submit 'Delete Spell' %>
    <% end %>
